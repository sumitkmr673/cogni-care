# The models, for the machine with the NVIDIA GPU. Several GB with the CUDA libraries.
-r requirements.txt

faster-whisper>=1.1
# CUDA 12 libraries (see app/cuda.py): cuBLAS + cuDNN for faster-whisper, and the CUDA runtime +
# cuBLAS that run.ps1 also hands to llama-server.
nvidia-cuda-runtime-cu12
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*

# Qwen does NOT run from pip: the prebuilt llama-cpp-python CUDA wheel needs AVX-512 and crashes on
# CPUs without it. run.ps1 starts llama.cpp's official llama-server instead - see README.